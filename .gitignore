# Local, editor and OS noise — the site itself is plain HTML/CSS with no build output.
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Timestamped pre-edit backups must never be served as site assets.
*.backup
*.bak
*.pre-*
